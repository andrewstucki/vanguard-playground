version: '3'

# if a task is referenced multiple times, only run it once
run: once

# configure bash to recursively expand **
shopt: [globstar]

tasks:
  generate:
    desc: "TODO"
    cmds:
    - echo "TODO"